import React from 'react';
import { Text } from 'react-native';

const DetailsDescription = (): React.ReactElement => {
  return <Text>Details Description</Text>;
};

export default DetailsDescription;
